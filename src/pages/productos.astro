---
import Layout from "../layouts/Layout.astro";

import Wave from "@/components/ui/contenido/Wave";

import WaveBottom from "@/components/ui/contenido/Wave-buttom";

import ScrollReveal from "@/components/ScrollReveal.astro";
import { Image } from "astro:assets";

import ki1 from "@/assets/productos/ki1.jpg";
import ki2 from "@/assets/productos/ki2.jpg";
import { lazy } from "astro:schema";

const filas = [
  {
    otro: "Solo materiales",
    hydrovision: "Solución integral",
  },
  {
    otro: "Sin estudio técnico",
    hydrovision: "Diagnóstico con drones",
  },
  {
    otro: "Proyectos lentos",
    hydrovision: "Instalación en menos de 7 días",
  },
  {
    otro: "Costos altos",
    hydrovision: "Hasta 40% menos",
  },
];

const opcionesKit1 = [
  {
    capacidad: "900 Lt",
    descripcion: "Pileta a 50m de distancia",
    precio: "S/. 1,600",
  },
  {
    capacidad: "1,200 Lt",
    descripcion: "Pileta a 100m de distancia",
    precio: "S/. 1,950",
    destacado: true,
  },
  {
    capacidad: "2,500 Lt",
    descripcion: "Pileta a 200m de distancia",
    precio: "S/. 4,200",
  },
];
const opcionesKit2 = [
  {
    capacidad: "10 m³",
    descripcion: "Geomembrana espesor 1.0mm",
    precio: "S/. 2,500",
  },
  {
    capacidad: "20 m³",
    descripcion: "Geomembrana espesor 1.0mm",
    precio: "S/. 3,400",
  },
  {
    capacidad: "40 m³",
    descripcion: "Geomembrana espesor 1.5mm",
    precio: "S/. 5,000",
    destacado: true,
  },
  {
    capacidad: "60 m³",
    descripcion: "Geomembrana espesor 1.5mm",
    precio: "S/. 8,800",
  },
];

const listKit2 = [
  {
    nombre: "Geomembrana HDPE 1.00mm/1.5mm",
  },
  {
    nombre: "Malla galvanizada electrosoldada",
  },
  {
    nombre: "04 und tubo acero reforzado",
  },
  {
    nombre: "Pernos sujetadores de geomembrana",
  },
  {
    nombre: "Candado de armado de estructura metálica",
  },
  {
    nombre: "Manguera corrugada y abrazadera Ubol",
  },
  {
    nombre: 'Brida de descarga de 2" - 3"',
  },
  {
    nombre: 'Codos de 90° de 2" - 3"',
  },
  {
    nombre: "Transporte, instalación y prueba in situ",
  },
];

const listKit1 = [
  {
    nombre: 'Línea de conducción tubería HDPE presión C 10 de 1" (100m)',
  },
  {
    nombre: '05 und codos de 1" y 05 und de 1" uniones',
  },
  {
    nombre: 'Válvula de paso de 1"',
  },
  {
    nombre: "Tanque almacenamiento rototanque o Rotoplas",
  },
  {
    nombre: 'Flotador mecánico de 1"',
  },
  {
    nombre: 'Salida inferior con llave y filtro de 1"',
  },
  {
    nombre: 'Salida de rebose de 1" superior',
  },
  {
    nombre: "Tubería para pileta y grifo de bronce",
  },
  {
    nombre: "Accesorios varios",
  },
];
---

<Layout title="Productos">
  <main class="w-full bg-white">
    <!-- Hero Minimalista -->
    <section class="relative py-16 sm:py-20 lg:py-32 overflow-hidden">
      <!-- Wave como fondo -->
      <div class="absolute inset-x-0 bottom-0 z-0 w-full">
        <ScrollReveal
          delay={200}
          animation="fade-left"
          intensity="medium"
          client:visible
        >
          <Wave client:visible />
        </ScrollReveal>
      </div>
      <!-- Contenido -->
      <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="max-w-4xl">
          <ScrollReveal
            delay={200}
            animation="fade-left"
            intensity="medium"
            client:visible
          >
            <p
              class="text-white font-medium mb-4 tracking-wide uppercase text-sm sm:text-sm bg-[#02B6D4] inline-block px-3 py-1 rounded-full"
            >
              Nuestros Kits
            </p>
          </ScrollReveal>
          <ScrollReveal
            delay={200}
            animation="fade-up"
            intensity="medium"
            client:visible
          >
            <h1
              class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-bold text-[#02B6D4] leading-tight mb-6"
            >
              Soluciones completas,
              <span class="text-gray-400">listas para instalar</span>
            </h1>

            <p class="text-lg sm:text-xl text-gray-500 max-w-2xl">
              Kits diseñados para comunidades altoandinas. Todo incluido:
              materiales, instalación y capacitación.
            </p>
          </ScrollReveal>
        </div>
      </div>
    </section>

    <!-- Línea divisoria -->
    <div class="w-full bg-white">
      <ScrollReveal
        delay={200}
        animation="fade-right"
        intensity="medium"
        client:visible
      >
        <WaveBottom client:visible />
      </ScrollReveal>
    </div>

    <!-- Kit 1: Consumo Humano -->
    <section class="py-12 sm:py-16 lg:py-32">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 sm:gap-10 lg:gap-20">
          <!-- Info -->
          <div class="lg:col-span-5">
            <ScrollReveal
              delay={200}
              animation="fade-left"
              intensity="medium"
              client:visible
            >
              <span
                class="text-5xl sm:text-6xl md:text-7xl lg:text-8xl xl:text-9xl font-black text-[#02B6D4] leading-none select-none"
                >01</span
              >
            </ScrollReveal>

            <div class="mt-4 sm:mt-6">
              <div class="flex items-center gap-2 sm:gap-3 mb-3 sm:mb-4">
                <ScrollReveal
                  delay={200}
                  animation="fade-up"
                  intensity="medium"
                  client:visible
                >
                  <h2
                    class="text-xl sm:text-2xl md:text-3xl lg:text-4xl font-bold text-gray-400"
                  >
                    Kit para Consumo Humano
                  </h2>
                </ScrollReveal>
              </div>
              <ScrollReveal
                delay={200}
                animation="fade-up"
                intensity="medium"
                client:visible
              >
                <p
                  class="text-base sm:text-lg text-gray-500 mb-6 sm:mb-8 lg:mb-10"
                >
                  Sistema completo de agua potable con tanque de almacenamiento
                  y pileta
                </p>
              </ScrollReveal>
            </div>

            <!-- Componentes -->
            <div class="space-y-4 mb-12">
              <ScrollReveal
                delay={200}
                animation="fade-up"
                intensity="medium"
                client:visible
              >
                <p
                  class="text-xs font-bold text-gray-400 uppercase tracking-wider"
                >
                  El kit incluye
                </p>
              </ScrollReveal>
              <ul class="space-y-3 text-gray-600">
                {
                  listKit1.map((item, index) => (
                    <ScrollReveal
                      delay={100 + index * 100}
                      animation="fade-up"
                      intensity="medium"
                      client:visible
                    >
                      <li class="flex items-start gap-3">
                        <span class="text-cyan-500 mt-1">›</span>
                        <span>{item.nombre}</span>
                      </li>
                    </ScrollReveal>
                  ))
                }
              </ul>
            </div>

            <a
              href="/contactanos"
              class="inline-flex items-center gap-2 text-cyan-600 font-semibold hover:text-cyan-700 transition-colors"
            >
              <span>Solicitar este kit</span>
              <svg
                class="w-4 h-4"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
              </svg>
            </a>
          </div>

          <!-- Precios -->
          <div class="lg:col-span-7">
            <div class="sticky top-8">
              <p
                class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-8"
              >
                Opciones disponibles
              </p>

              <div class="space-y-6">
                {
                  opcionesKit1.map((opcion, index) => (
                    <ScrollReveal
                      delay={100 + index * 100}
                      animation="fade-up"
                      intensity="medium"
                      client:visible
                    >
                      <div class="group p-6 border border-gray-100 rounded-2xl hover:border-cyan-200 hover:bg-cyan-50/30 transition-all duration-300">
                        <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                          <div>
                            <h3 class="text-xl font-bold text-[#02B6D4] mb-1">
                              Capacidad {opcion.capacidad}
                            </h3>
                            <p class="text-gray-500">{opcion.descripcion}</p>
                          </div>
                          <div class="text-right">
                            <p class="text-3xl font-bold text-gray-400">
                              {opcion.precio}
                            </p>
                          </div>
                        </div>
                      </div>
                    </ScrollReveal>
                  ))
                }
                <!-- Imagen placeholder -->
                <ScrollReveal
                  delay={300}
                  animation="fade-up"
                  intensity="medium"
                  client:visible
                >
                  <div
                    class="mt-12 aspect-video rounded-2xl bg-gray-100 overflow-hidden"
                  >
                    <Image
                      src={ki1}
                      alt="Kit para consumo humano"
                      class="w-full h-full object-cover"
                      loading="lazy"
                      onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"
                    />
                    <div
                      class="hidden w-full h-full items-center justify-center bg-gradient-to-br from-cyan-50 to-blue-50"
                    >
                      <div class="text-center">
                        <span class="text-6xl">💧</span>
                        <p class="text-gray-400 mt-4 text-sm">Imagen del kit</p>
                      </div>
                    </div>
                  </div>
                </ScrollReveal>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Línea divisoria -->
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div
          class="h-px bg-gradient-to-r from-transparent via-gray-200 to-transparent"
        >
        </div>
      </div>

      <!-- Kit 2: Geotanque para Animales -->
      <section class="py-12 sm:py-16 lg:py-32">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div
            class="grid grid-cols-1 lg:grid-cols-12 gap-8 sm:gap-10 lg:gap-20"
          >
            <!-- Precios (orden invertido en desktop) -->
            <div class="lg:col-span-7 order-2 lg:order-1">
              <div class="sticky top-8">
                <p
                  class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-8"
                >
                  Opciones disponibles
                </p>

                <div class="space-y-6">
                  {
                    opcionesKit2.map((opcion, index) => (
                      <ScrollReveal
                        delay={100 + index * 100}
                        animation="fade-up"
                        intensity="medium"
                        client:visible
                      >
                        <div class="group p-6 border border-gray-100 rounded-2xl hover:border-emerald-200 hover:bg-emerald-50/30 transition-all duration-300">
                          <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                            <div>
                              <h3 class="text-xl font-bold text-[#02B6D4] mb-1">
                                Capacidad {opcion.capacidad}
                              </h3>
                              <p class="text-gray-500">{opcion.descripcion}</p>
                            </div>
                            <div class="text-right">
                              <p class="text-3xl font-bold text-gray-400">
                                {opcion.precio}
                              </p>
                            </div>
                          </div>
                        </div>
                      </ScrollReveal>
                    ))
                  }
                </div>

                <!-- Imagen placeholder -->
                <ScrollReveal
                  delay={300}
                  animation="fade-up"
                  intensity="medium"
                  client:visible
                >
                  <div
                    class="mt-12 aspect-video rounded-2xl bg-gray-100 overflow-hidden"
                  >
                    <Image
                      src={ki2}
                      alt="Kit geotanque para animales"
                      class="w-full h-full object-cover"
                      loading="lazy"
                      onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"
                    />
                    <div
                      class="hidden w-full h-full items-center justify-center bg-gradient-to-br from-emerald-50 to-teal-50"
                    >
                    </div>
                  </div>
                </ScrollReveal>
              </div>
            </div>

            <!-- Info -->
            <div class="lg:col-span-5 order-1 lg:order-2">
              <ScrollReveal
                delay={200}
                animation="fade-right"
                intensity="medium"
                client:visible
              >
                <span
                  class="text-5xl sm:text-6xl md:text-7xl lg:text-8xl xl:text-9xl font-black text-[#02B6D4] leading-none select-none"
                  >02</span
                >
              </ScrollReveal>

              <div class="mt-4 sm:mt-6">
                <div class="flex items-center gap-2 sm:gap-3 mb-3 sm:mb-4">
                  <ScrollReveal
                    delay={200}
                    animation="fade-up"
                    intensity="medium"
                    client:visible
                  >
                    <h2
                      class="text-xl sm:text-2xl md:text-3xl lg:text-4xl font-bold text-gray-400"
                    >
                      Kit Geotanque para Animales
                    </h2>
                  </ScrollReveal>
                </div>
                <ScrollReveal
                  delay={200}
                  animation="fade-up"
                  intensity="medium"
                  client:visible
                >
                  <p
                    class="text-base sm:text-lg text-gray-500 mb-6 sm:mb-8 lg:mb-10"
                  >
                    Sistema de almacenamiento con geomembrana para abrevadero de
                    ganado
                  </p>
                  <ScrollReveal
                    delay={200}
                    animation="fade-right"
                    intensity="medium"
                    client:visible
                  />

                  <!-- Componentes -->
                  <div class="space-y-4 mb-12">
                    <ScrollReveal
                      delay={200}
                      animation="fade-up"
                      intensity="medium"
                      client:visible
                    >
                      <p
                        class="text-xs font-bold text-gray-400 uppercase tracking-wider"
                      >
                        El kit incluye
                      </p>
                    </ScrollReveal>
                    <ul class="space-y-3 text-gray-600">
                      {
                        listKit2.map((item, index) => (
                          <ScrollReveal
                            delay={100 + index * 100}
                            animation="fade-right"
                            intensity="medium"
                            client:visible
                          >
                            <li class="flex items-start gap-3">
                              <span class="text-emerald-500 mt-1">›</span>
                              <span>{item.nombre}</span>
                            </li>
                          </ScrollReveal>
                        ))
                      }
                    </ul>
                  </div>
                  <a
                    href="/contactanos"
                    class="inline-flex items-center gap-2 text-emerald-600 font-semibold hover:text-emerald-700 transition-colors"
                  >
                    <span>Solicitar este kit</span>
                    <svg
                      class="w-4 h-4"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
                    </svg>
                  </a>
                </ScrollReveal>
              </div>
            </div>
          </div>

          <!-- Sección de Diferenciación -->
          <section class="py-16 sm:py-20 lg:py-32 bg-white">
            <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
              <!-- Header -->
              <div class="text-center mb-12 sm:mb-16 lg:mb-20">
                <p
                  class="text-white font-medium mb-4 tracking-wide uppercase text-xs sm:text-sm bg-[#02B6D4] inline-block px-5 py-2 rounded-full"
                >
                  La diferencia
                </p>
                <h2
                  class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl xl:text-6xl font-bold text-[#02B6D4]"
                >
                  ¿Por qué elegir <span class="text-gray-400">HYDROVISION</span
                  >?
                </h2>
              </div>

              <!-- Tabla comparativa minimalista -->
              <div class="overflow-hidden">
                <!-- Headers -->
                <div class="grid grid-cols-2 gap-4 sm:gap-8 mb-8 min-w-[300px]">
                  <div class="text-center">
                    <span
                      class="text-xs sm:text-sm font-bold text-gray-400 uppercase tracking-wider"
                      >Otros</span
                    >
                  </div>
                  <div class="text-center">
                    <span
                      class="text-xs sm:text-sm font-bold text-cyan-600 uppercase tracking-wider"
                      >HYDROVISION</span
                    >
                  </div>
                </div>

                <!-- Filas -->
                <div class="space-y-4 min-w-[300px]">
                  <!-- Fila 1 -->

                  {
                    filas.map((fila, index) => (
                      <ScrollReveal
                        delay={100 + index * 100}
                        animation="fade-up"
                        intensity="medium"
                        client:visible
                      >
                        <div class="grid grid-cols-2 gap-4 sm:gap-8">
                          <div class="flex items-center justify-center gap-2 sm:gap-3 py-3 sm:py-5 px-2 sm:px-4 border-b border-gray-100">
                            <span class="text-red-600">✗</span>
                            <span class="text-red-500 text-center text-xs sm:text-base">
                              {fila.otro}
                            </span>
                          </div>
                          <div class="flex items-center justify-center gap-2 sm:gap-3 py-3 sm:py-5 px-2 sm:px-4 border-b border-green-300 bg-green-50/30 rounded-lg">
                            <span class="text-green-600 font-bold">✓</span>
                            <span class="text-green-500 font-semibold text-center text-xs sm:text-base">
                              {fila.hydrovision}
                            </span>
                          </div>
                        </div>
                      </ScrollReveal>
                    ))
                  }
                </div>
              </div>
            </div>
          </section>
        </div>

        <!-- CTA Final Minimalista -->
        <section class="py-16 sm:py-20 lg:py-24 bg-gray-50">
          <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <h2
              class="text-xl sm:text-2xl md:text-3xl lg:text-4xl font-bold text-gray-900 mb-3 sm:mb-4"
            >
              ¿Necesitas un kit personalizado?
            </h2>
            <p
              class="text-base sm:text-lg lg:text-xl text-gray-500 mb-8 sm:mb-10"
            >
              Evaluamos tu proyecto y diseñamos la solución perfecta para tu
              comunidad
            </p>
            <a
              href="/contactanos"
              class="inline-flex items-center gap-2 sm:gap-3 bg-gray-900 text-white font-semibold px-6 sm:px-8 py-3 sm:py-4 rounded-full hover:bg-gray-800 transition-colors text-sm sm:text-base"
            >
              <span>Contactar a un experto</span>
              <svg
                class="w-5 h-5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
              </svg>
            </a>
          </div>
        </section>
      </section>
    </section>
  </main>
</Layout>
